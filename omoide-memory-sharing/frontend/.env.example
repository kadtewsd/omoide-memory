# バックエンド API のエンドポイント URL
# ローカル開発時は通常 http://localhost:8080
VITE_API_URL=http://localhost:8080

# Vite サーバーでアクセスを許可するホスト名（カンマ区切り）
# LAN 内の他端末からアクセスする場合などに指定します（例: localhost,127.0.0.1,.local）
VITE_ALLOWED_HOSTS=localhost,127.0.0.1,.local
