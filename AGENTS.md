# AGENTS.md - omoide-memory 開発ガイドライン (全体共通)

本ドキュメントは `omoide-memory` リポジトリ全体の共通ガイドラインです。
サブプロジェクトごとに固有のルールが定義されています。

---

## ディレクトリ別ルール参照ガイド

作業するプロジェクト/ディレクトリに応じて、以下の `AGENTS.md` が適用されます。

| ディレクトリ | 適用される規約 | 主な対象技術 |
|---|---|---|
| `backend/`, `omoide-memory-sharing/backend/` | `backend/AGENTS.md` | Spring Boot, jOOQ, R2DBC, WebFlux, Coroutines, Arrow-kt |
| `frontend/`, `omoide-memory-sharing/frontend/` | `frontend/AGENTS.md` | React 19, TypeScript, React Hooks, Zod |
| モバイルアプリ（Android） | モバイル用 `AGENTS.md` | Jetpack Compose, Dagger Hilt, Room |

---

## リポジトリ全体共通の設計原則

1. **DRY原則と高凝集・低結合の徹底**
   - 制御結合（Control Coupling: フラグ変数や内部状態の `if` 分岐）を避けること。
   - **制御結合の解消アプローチ（優先順位）**:
     関数やコンポーネント内部で「何をするか／どの型を生成するか」を分岐させるような制御結合が発生しそうな場合、以下の優先順位で設計を検討すること：
     1. **まずは呼び出し元で決められるかを考える（高階関数・関数オブジェクト・委譲）**:
        関数内部で分岐（`when` や `if`）してインスタンス生成や振る舞いを決定せず、呼び出し元から高階関数（ファクトリ関数やラムダ）を受け取り、純粋に抽出・適用する責務に専念させる（例: `List<WorkInfo>.requestType(create = { progress -> ... })`）。
     2. **決められなければ 代数的データ型（Algebraic Data Types: Sealed Class / Sealed Interface）**:
        どうしても呼び出し側で完結できず、状態や種別を抽象化して表現する必要がある場合は、型安全な代数的データ型を定義してパターンマッチを行う。ただし、内部で無意味にパターンマッチしてインスタンスを作るような設計は避け、状態自体の表現に留めること（これは最悪ではないが、呼び出し元で決められるなら 1 が勝る。よくはない例）。
     3. **最悪フラグ（本当にどうしようもない時のみ）**:
        Boolean などのフラグ変数を渡して内部の振る舞いを切り替える設計は最も結合度が高く保守性を損なうため、本当にどうしようもない最終手段とする。
   - `if` 文を使ってコンポーネント内部でちょっとした分かりやすさや UX を作ろうとせず、`if` 分岐を無くす方向性で設計を検討すること。
   - 表示文言や制御値はコンポーネント側で自動判定せず、呼び出し元に決定させる設計を徹底すること。
   - 補助機能やログ出力等の実装において、`?.let` やエルビス演算子（`?:`）を使った無意味・過剰なヌル判定分岐を行わないこと。コードのシンプルさ・直感的な分かりやすさを最優先とすること。
   - 一時変数（メッセージやフラグ等）に一度代入してから `if` 判定・呼び出しを行うくせをなくし、多少冗長であっても `if` 分岐を削り、`when` 節などで直接呼び出しや処理を行うこと。
   - **分岐を嫌うこと。合成しろ**:
     Map や List などのデータ構造や処理を `if/else` で丸ごと分岐させて重複記述するのをやめ、ベースとなる構造に必要な差分要素をコレクションの合成（`+` 演算子）や関数合成で組み立てること（例: `mapOf(...) + if (condition == null) emptyMap() else mapOf(...)`）。
   - **無駄な分岐の徹底排除（ログのための分岐禁止）**:
     分岐してログ（「〜が見つからなかったためスキップします」等）を出すくらいであれば、大した意味もないので出さないこと。あるいは正常系でログを出して異常系は出ないことで自ずと分かるようにするなど、無駄な分岐を嫌うこと。
   - **無意味な1行委譲ラッパー関数の禁止**:
     単に内部の別関数を1行呼び出すだけのような無意味なラッパー関数（オーバーロード）を作らないこと。
   - **環境変数は原則として Adapter（外郭層）で対処すること（結合度を下げる）**:
     環境変数（`System.getenv` 等）へのアクセスは原則として Adapter などの外郭層で行い、内部の処理・サービス・オブジェクトへは引数経由で受け渡すこと。内部が環境変数へ直接アクセスすると結合度が高くなり保守性やテスト容易性を損なうため、結合度は下げるべきである。引数の数が極端に増えてどうしても不恰好になる場合のみ例外的に内部でのアクセスを許容するが、基本原則として Adapter で対処すること。
   - **わかるものは呼び出し元で早くわからせる設計を好むこと**:
     呼び出し元ですでに判明している情報（例: 値が NULL かどうか、特定の条件を満たしているかどうか等）を、わざわざ関数の引数に NULL のまま渡して内部で判定・分岐させる設計（制御結合）を排除すること。呼び出し元で判定して、条件を満たしたときのみ非 Null な引数で関数を呼び出すなど、呼び出し側で早期に自明にさせ、呼ばれる側の関数の責務と引数を極限まで純粋かつシンプルに保つこと。
2. **定数化の絶対禁止ルール（1度しか使わないものは絶対に定数化するな・ローカル変数で具体的に表現せよ）**
   - **【厳格遵守・絶対禁止】一度しか使わない固定文字列やリテラルの無駄な定数化を徹底禁止**:
     定数（`const val`、トップレベル `val`、`companion object` 内の `val` など）の価値は「**複数箇所から実際に参照・再利用されるとき**」にのみ存在する。
     **固定文字列を定数にしたからといって、コードが分かりやすくなるわけではない。** 定義元と利用箇所が物理的に離れることで視線移動が増え、かえって認知負荷を増大させるだけである。
     1箇所でしか使わないリテラル（例: FCM や各種外部 API のエンドポイント URL、JSON キー、環境変数名、フォーマット文字列など）を、ファイル上部や companion object に切り出して無意味に定数化してはならない。
   - **定数にするのではなく、利用箇所で一度バインドしたローカル変数で「なに」かを明示する**:
     対象が「なに」であるかを表現したいのであれば、固定文字列を定数化するのではなく、**利用箇所で一度ローカル変数にバインドし、その変数名で具体的に表現すること**。
     その際、単なる `url` や `data` といった抽象的・曖昧な変数名では「なに」なのかが分からない。普通に **「FCM のエンドポイントであること」を明示した具体的な変数名（例: `fcmEndpointUrl`）にすること**。
   - **【アンチパターンと正しい例】**:
     - ❌ **最悪な例 1（固定文字列を定数化してしまう / 惰性・形式主義）**:
       ```kotlin
       // 固定文字列を定数にしても分かりやすくなるわけではない（完全禁止！）
       private const val FCM_ENDPOINT_TEMPLATE = "https://fcm.googleapis.com/v1/projects/%s/messages:send"

       fun postToFcm(projectId: String, ...) {
           val url = URL(FCM_ENDPOINT_TEMPLATE.format(projectId))
           ...
       }
       ```
     - ❌ **悪い例 2（インライン化したが、変数名が「url」で「なに」かが分かりづらい）**:
       ```kotlin
       fun postToFcm(projectId: String, ...) {
           // 単に「url」では何のエンドポイントなのかが伝わらない
           val url = URL("https://fcm.googleapis.com/v1/projects/%s/messages:send".format(projectId))
           ...
       }
       ```
     - ✅ **正しい例（定数化せず、利用箇所で FCM のエンドポイントであることを明示した変数名にバインドして表現する）**:
       ```kotlin
       fun postToFcm(projectId: String, ...) {
           // FCM のエンドポイントであることを明示した変数名でバインドして表現する
           val fcmEndpointUrl = URL("https://fcm.googleapis.com/v1/projects/%s/messages:send".format(projectId))
           (fcmEndpointUrl.openConnection() as HttpURLConnection).apply { ... }
       }
       ```
   - **「なんとなく定数化」「説明のための定数化」の完全根絶**:
     - 「ファイル上部に定数としてまとめたほうが行儀が良さそうに見える」「後から変えるかもしれない」という惰性・形式主義・ばかげた配慮を完全に排除すること。
     - 定数を定義してよい唯一の条件は **「実際に 2 箇所以上の異なる場所から参照・共有されていること」** のみ。
     - 1箇所でしか使わないものは、利用箇所で意図を語る具体的で明瞭な名前のローカル変数に直接バインドして書くこと。
   - ※ マジックナンバーや非直感的な計算ロジック（バイト数計算、特殊な閾値など、複数箇所または意味が不明瞭な計算式）に限り、名前付き定数化してドキュメントコメントに背景を明記することを許容する。
3. **コミット / PR 作成ルール**
   - 関連ドキュメントやPR説明文は必要に応じて `ai_script/` 配下に保存すること。
4. **Gradle 実行・ビルド検証に関するルール**
   - ユーザーから明示的な指示がない限り、Gradle（`./gradlew` によるビルド、テスト、ktlint等）は実行しなくて良い（実行しないこと）。
5. **関数の引数におけるデフォルト値の禁止（プロダクションコード）**
   - プロダクションコードにおいて関数のデフォルト引数（デフォルト値）の使用は禁止とする。
   - モバイルの UI コンポーネント（Jetpack Compose 等）である場合を除き、引数は原則として呼び出し元から明示的に指定させること。
6. **名前付き引数（Named Arguments）の強制ルール**
   - 関数の引数が 2 つ以上存在する場合、呼び出し元では必ず名前付き引数（`key = value`）を使用すること。
7. **引数および状態における NULL 不許可の原則**
   - 関数の引数は原則として **NULL 不許可（Non-Null 型）** とする。安易に Nullable 型（`T?`）を引数に取らず、呼び出し元に責任を持って非 Null な値を生成・渡させること。
   - 状態表現においても安易に `null` を使わず、Enum や Sealed Class などの型として明示的に状態（例: `IDLE`, `MAIN`, `NONE` 等）を定義・表現すること。
   - 「`null` なら初期状態」「`null` なら通常動作」「`null` ならスキップ」といった `null` に暗黙の意味を持たせる設計を排除し、非 Null な型としてモデリングすることで、`when` 節でのパターンマッチを完全網羅（exhaustive）させ、分岐漏れや不整合のリスクを構造的に防止すること。
8. **when 式での同一振る舞いのまとめ方（横並び全列挙）**
   - `when` 式でパターンマッチを行う際、複数の状態・Enum 値に対する振る舞い（処理内容）が同一である場合は、縦に各ケースを並べて同じコードを重複させず、カンマ区切りで横に並べてまとめること。
   - すでにインスタンス自身が値（プロパティなど）を持っている場合は、ブランチ内で静的に Enum 定数を再度指定するような冗長な記述を避け、対象インスタンスのプロパティを直接参照して処理すること。
9. **Dao っぽい永続化の禁止とドメイン責務の分離**
   - **Dao っぽい永続化の禁止**: Repository のメソッド引数にテーブルのカラム値をバラバラに受け渡す設計（低レベルな `updateProgress(id, point, count, updatedAt, message)` 等）を禁止する。永続化単位は原則としてドメインオブジェクト（Entity）そのものを受け渡し、`add(entity)` や `update(entity)` として実装すること。
   - **Repository にドメインの責務を入れることの禁止**: 日時生成（`updatedAt` / `createdAt` の設定）やメッセージの生成・判定、Enum/State の遷移といったビジネスロジック・ドメイン責務を Repository に持たせてはならない。これらはすべてドメインオブジェクト自身の初期化・遷移メソッドで行い、Repository は単なる永続化（Dao との仲介）に専念させること。
   - **状態変化は Domain オブジェクトが担う**: オブジェクトの状態変更やステップ進行は、ドメインオブジェクト自身に `next(...)` 等のメソッドを定義して行わせること。
   - **単一性と内部での状態ハードコード禁止**: ステップ進行を担うようなことをしたいときは、変化する進捗件数のみを受け取る単一シグネチャ（例: `fun next(processedCount: Int): DomainObject`）とし、無駄なオーバーロード（引数なし、メッセージ付き、ポイント指定等）の乱立を禁止する。また、変更メソッドの内部で特定のステータス名（`Start` や `TargetEmpty` 等）を判定する `if` 条件分岐をハードコードしてはならない。カプセル化とポリモーフィズムを徹底し、次のステップへの遷移はドメインオブジェクト自身の内部状態定義に委譲して自己完結して決定すること。
   - **initial メソッドの単一性と明確な命名 (totalRequestCount)**: 初期化ファクトリ（`initial`）において、初期ステップ状態は「とにかく始まったら `Start`（要求開始）」であり最初から無条件に自明に決まっているため、引数で受け取ったり内部で件数による `if` 分岐（0件だからといって `TargetEmpty` を返すなど）を行ってはならない。対象が 0 件の場合でも必ず `Start` から開始し、Empty 判定を経て `TargetEmpty` または `WorkerStarted`（ワーカー起動・処理開始）へとステップを進めること。受け取るべき引数は、処理予定の総数を明確に表す `totalRequestCount`（アップロード・削除する予定のトータルの件数）のみとする。意味が曖昧な `targetCount` は廃止し、`totalRequestCount` と命名すること。
   - **メッセージへの動的変数埋め込みの禁止と分岐の排除**: レポートやステータスのメッセージに変数を埋め込むこと（例: `"[$count/$total] ..."` や `"対象件数: $count 件"` 等）を禁止する。進捗件数や総件数はすでに専用のカラム/プロパティ（`successCount`, `totalRequestCount` 等）として保持されているため、メッセージに重複して埋め込む必要はない。レポートの本質は「どのステップ・状態まで到達したか」を記録することであり、メッセージは Enum や Sealed Class に定義された静的な固定文言（`lastPoint.message`）をそのまま代入すること。これにより、メッセージ生成のための無駄な `if` や `when` 条件分岐を根絶し、コードを極限までシンプルに保つこと。
   - **契約による設計（Design by Contract）の徹底と不要なガード節の排除**: 呼び出し元がプロセスの順序や事前条件（制約）を担保してメソッドを呼び出す設計（契約による設計）を徹底すること。ドメインオブジェクト内部で呼び出し元の呼び出しシーケンスを疑い、「現在どの状態か」「上限に達したか」といった余計なガード節（`if` や `when` 分岐）を設けてはならない。ステップ進行は単に「次が分かればよい」のであり、定義された状態遷移（`this.lastPoint = this.lastPoint.next`）を無条件に適用し、本質的に必要なプロパティ更新のみを行うこと。
   - **「今の状態」が語る原則と無駄な中断/エラーステータスの排除（ばかげた親切心の禁止）**: `fun interrupted(...)` や `fun unexpectedError(...)` のような、失敗中断や予期せぬ例外発生専用のステータス遷移メソッドやエラーステータス定義（`Interrupted`, `UnexpectedError` 等）を作成してはならない。レポートにおいて「今の状態（`lastPoint`）」および進捗件数が分かれば「どこで遮られたか」は一目瞭然である。失敗時に汎用的なエラー中断ステータスで上書きしてしまうと、本来残るべき「どのステップの途中で処理が遮られたのか」という貴重な情報が失われてしまう。「ばかげた親切心」で余計なエラーステータスや遷移メソッドを生み出すことを禁止し、状態は直前のステップのまま自然に止める設計を徹底すること。
   - **YAGNI 原則の徹底と不要なオーバーライド（equals / hashCode 等）の禁止**: 比較（同値性判定や Set/Map などのコレクションキー利用等）を行わないクラス・Entity において、漫然と `equals` や `hashCode` をオーバーライドしてはならない。なぜなら比較しないからである。「とりあえず」「Entityだから念のため」といったばかげたオーバーライドは一切不要であり、YAGNI（You Aren't Gonna Need It）を最優先すること。
10. **単一責任の原則（SRP）の徹底と「ほどよい大きさ」のモデリング**
    - **責務の混在を排除する（SRP の重視）**:
      1 つのクラス・オブジェクトに無関係な複数の責務を詰め込んではならない。
      （例: PUSH 通知の送信処理の中に、「PUSH 通知の送信」「アイコン画像の生成・リサイズ」「Service Account からのアクセストークン取得」などが混在している状態は責務過多である）。
    - **過剰な細分化の防止（「ほどよい大きさ」でまとめる）**:
      細かすぎてもコードが散らかり煩雑になるため、「PUSH 通知をするための構成要素（`PushNotification`）」のように、ドメインや処理の目的に応じた「ほどよい大きさ」のクラス・オブジェクトで関連する要素（トークン取得、アイコン生成など）をまとめること。
    - **呼び出し先でログをパッパと出さず、Either を好む（ログ責務の分離）**:
      呼び出し先（内部処理・下位レイヤー・小さなプライベート関数など）で安易にログ（`logger.info` や `logger.warn` 等）を出力してはならない。
      ログを出したいのであれば、メインルーチンの外側（呼び出し元・上位レイヤー）で結果として出せばよい。呼び出し先の小さな関数の中でログを出してはならない。
      成否やエラー情報は `Either`（`Either<Error, Value>`）で表現して呼び出し元に返し、ログを出力したくなったら呼び出し元の判断で出力させる設計を徹底すること。
    - **先行走査・重複ループ・不要なプライベート関数の切り出し禁止（メインループに相乗りする）**:
      すでにメインルーチンでコレクションの走査（`forEach` 等）を行っている場合、そのループの前で別個にプライベート関数を切り出してコレクションを先行走査・一括取得するような設計は、二重ループと無駄な複雑さを生むだけである。
      メインループの中で必要な値を取得（相乗り）すればよく、わざわざ小さなプライベート関数を乱立させて事前にまとめて取得しようとしてはならない。




